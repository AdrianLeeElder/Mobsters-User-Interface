server {
  listen       3000 default_server;
  server_name  mobsters-dev.adriansoftware.com
  root         /usr/share/nginx/html;
  # location / {
  # }
  # Redirect non-https traffic to https
  if ($scheme != "https") {
    return 301 https://$host$request_uri;
  }
}
